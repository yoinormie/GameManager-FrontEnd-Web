import 'package:ecommerce/app.dart';
import 'package:flutter/material.dart';

void main() {
  // Todo lo que tenemos que hacer antes de el backend
  // Add widgets binding
  // Init Local Storage
  // Await Native Splash
  // Initialize Firebase
  // Initialize Authentication
  runApp(const App());
}
